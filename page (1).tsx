import { ProductCard } from '@/components/product-card'; import { api } from '@/lib/api';
export default async function Shop({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){ const sp=await searchParams; const qs=new URLSearchParams(); if(sp.category) qs.set('category',sp.category); if(sp.q) qs.set('q',sp.q); const res=await api.products(`?${qs}`); return <section className="mx-auto max-w-7xl px-4 py-10"><h1 className="mb-8 text-4xl font-bold">Shop Collection</h1><div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-4">{res.data.products.map((p:any)=><ProductCard key={p.id} p={p}/>)}</div></section> }
