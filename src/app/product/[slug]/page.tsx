import Image from 'next/image'; import { api } from '@/lib/api'; import { money } from '@/lib/money'; import { AddToCart } from './ui';
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){ const {slug}=await params; const {data}=await api.product(slug); const p=data.product; return <section className="mx-auto grid max-w-7xl gap-10 px-4 py-10 md:grid-cols-2"><div className="relative aspect-[4/5] overflow-hidden rounded-[2rem]"><Image src={p.image} alt={p.name} fill className="object-cover"/></div><div className="space-y-5"><p className="text-primary">{p.category}</p><h1 className="text-4xl font-bold">{p.name}</h1><p className="text-lg opacity-75">{p.description}</p><b className="block text-2xl">{money(p.price)}</b><p className="text-sm opacity-70">Stock: {p.stock}</p><AddToCart productId={p.id}/></div></section> }
