'use client';
import Link from 'next/link'; import { ShoppingBag, SunMoon } from 'lucide-react'; import { useTheme } from 'next-themes'; import { useQuery } from '@tanstack/react-query'; import { api } from '@/lib/api';
export function Header(){ const {setTheme,theme}=useTheme(); const {data}=useQuery({queryKey:['cart'],queryFn:api.cart,retry:false}); const count=data?.data?.cart?.items?.reduce((s:number,i:any)=>s+i.quantity,0)??0;
return <header className="fixed inset-x-0 top-0 z-50 border-b border-border bg-background/90 backdrop-blur"><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-4"><Link href="/" className="font-serif text-2xl font-bold tracking-wide">Classic Closet</Link><nav className="hidden gap-8 md:flex"><Link href="/shop">Shop</Link><Link href="/shop?category=Apparel">Apparel</Link><Link href="/shop?category=Accessories">Accessories</Link><Link href="/orders">Orders</Link></nav><div className="flex items-center gap-3"><button onClick={()=>setTheme(theme==='dark'?'light':'dark')} className="rounded-full border border-border p-2"><SunMoon size={18}/></button><Link href="/cart" className="relative rounded-full border border-border p-2"><ShoppingBag size={18}/>{count>0&&<span className="absolute -right-2 -top-2 rounded-full bg-primary px-2 text-xs text-primaryForeground">{count}</span>}</Link><Link href="/login" className="rounded-full bg-primary px-4 py-2 text-sm font-semibold text-primaryForeground">Sign in</Link></div></div></header> }
