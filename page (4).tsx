'use client';
import { useState } from 'react'; import { useMutation } from '@tanstack/react-query'; import { api } from '@/lib/api';
export default function Checkout(){ const [method,setMethod]=useState<'MPESA'|'PAYSTACK'>('MPESA'); const [orderId,setOrderId]=useState(''); const [phone,setPhone]=useState(''); const checkout=useMutation({mutationFn:(body:any)=>api.checkout(body),onSuccess:(r)=>setOrderId(r.data.order.id)}); const pay=useMutation({mutationFn:async()=>{ if(method==='PAYSTACK'){ const r=await api.paystack(orderId); location.href=r.data.authorizationUrl; } else await api.mpesa(orderId,phone); }});
function submit(e:any){e.preventDefault(); const f=new FormData(e.currentTarget); checkout.mutate({paymentMethod:method,shippingAddress:{firstName:f.get('firstName'),lastName:f.get('lastName'),phone:f.get('phone'),email:f.get('email'),address1:f.get('address1'),city:f.get('city'),county:f.get('county'),country:'Kenya'}})}
return <section className="mx-auto max-w-3xl px-4 py-10"><h1 className="mb-6 text-4xl font-bold">Secure Checkout</h1>{!orderId?<form onSubmit={submit} className="grid gap-4 rounded-3xl border border-border bg-card p-6 sm:grid-cols-2"><input name="firstName" required placeholder="First name" className="rounded-xl border border-border bg-background p-3"/><input name="lastName" required placeholder="Last name" className="rounded-xl border border-border bg-background p-3"/><input name="email" required type="email" placeholder="Email" className="rounded-xl border border-border bg-background p-3"/><input name="phone" required placeholder="Phone" className="rounded-xl border border-border bg-background p-3"/><input name="address1" required placeholder="Delivery address" className="rounded-xl border border-border bg-background p-3 sm:col-span-2"/><input name="city" required placeholder="City" className="rounded-xl border border-border bg-background p-3"/><input name="county" placeholder="County" className="rounded-xl border border-border bg-background p-3"/><select value={method} onChange={e=>setMethod(e.target.value as any)} className="rounded-xl border border-border bg-background p-3 sm:col-span-2"><option value="MPESA">M-Pesa STK</option><option value="PAYSTACK">Paystack card/mobile money</option></select><button className="rounded-full bg-primary px-6 py-3 font-bold text-primaryForeground sm:col-span-2">Create order</button></form>:<div className="rounded-3xl border border-border p-6"><p className="mb-4">Order created. Complete payment.</p>{method==='MPESA'&&<input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="2547XXXXXXXX" className="mb-4 w-full rounded-xl border border-border bg-background p-3"/>}<button onClick={()=>pay.mutate()} className="rounded-full bg-primary px-6 py-3 font-bold text-primaryForeground">Pay now</button></div>}</section> }
